import 'package:flutter/material.dart';

class ActiveTripPanel extends StatelessWidget {
  static const routeName = 'map/active_trip/';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
